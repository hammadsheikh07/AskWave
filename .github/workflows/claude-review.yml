name: Auto Review

on:
  pull_request:
    types: [opened, ready_for_review, reopened]

jobs:
  trigger-review:
    runs-on: ubuntu-latest
    steps:
      - name: Comment @claude on PR
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          gh pr comment ${{ github.event.pull_request.number }} \
            --repo ${{ github.repository }} \
            --body "@claude"
